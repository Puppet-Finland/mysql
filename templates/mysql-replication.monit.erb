### THIS FILE IS MANAGED BY PUPPET. ANY MANUAL CHANGES WILL GET OVERWRITTEN.
#
check program mysql-replication with path <%= scope.lookupvar('monit::params::fragment_dir') %>/mysql-replication.sh
   if status != 0 timeout 10 seconds for 5 cycles then alert
   alert <%= scope.lookupvar('mysql::monit::slave::monitor_email') %>
